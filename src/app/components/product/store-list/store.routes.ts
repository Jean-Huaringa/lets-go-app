
import { TiendaListComponent } from '../store-list/store-list.component';
import { RouterModule, Routes } from '@angular/router'; 

export const routes: Routes = [
  
   { path: '', component: TiendaListComponent },
]
